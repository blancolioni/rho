package body Rho.UI.Main is

   ----------
   -- Init --
   ----------

   procedure Init
     (Handle : Rho.Handles.Handle)
   is
   begin
      Local_UI_Handle := Handle;
   end Init;

end Rho.UI.Main;
